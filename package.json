{
  "name": "freebind",
  "version": "0.2.2",
  "description": "bind sockets to random IP addresses from specified prefixes",
  "keywords": ["anyip", "rate-limit", "socket", "ipv6", "ratelimiting"],
  "type": "module",
  "main": "index.js",
  "repository": "https://github.com/imputnet/freebind.js.git",
  "author": "dumbmoron <log@riseup.net>",
  "license": "AGPL-3.0",
  "private": false,
  "scripts": {
    "postinstall": "[[ $(uname -s) != Linux ]] || [[ $npm_execpath != */bin/bun ]] || ([ -f node_modules/syscall-napi/build ] || cd node_modules/syscall-napi && bun i)"
  },
  "dependencies": {
    "ipaddr.js": "2.3.0",
    "node-gyp": "^12.2.0",
    "syscall-napi": "0.0.6",
    "undici": "^6.22.0"
  }
}
